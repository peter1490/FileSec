/target
**/*.rs.bk
*.pdb

# Packaging / release build outputs (produced in CI)
/dist
*.dmg
*.msi
*.deb
*-setup.exe

# FileSec runtime artifacts that must never be committed
*.fsec
*.fsecpub
/scratch

# macOS Finder metadata
.DS_Store

# Local signing secrets — never commit
.env
